language: minimal
services:
  - docker
install:
  - docker build -t nodejs-test -f test/nodejs.Dockerfile .
  - docker build -t deno-test -f test/deno.Dockerfile .
script:
  - docker run deno-test
  - docker run nodejs-test
